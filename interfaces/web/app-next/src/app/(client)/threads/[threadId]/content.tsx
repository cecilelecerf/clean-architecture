import { ThreadId } from "@infrastructure/types/thread";
import { UserDto } from "@infrastructure/types/user";
type Props = { threadId: ThreadId, userId: UserDto["id"] }
export const TestContent = ({ threadId, userId }: Props) => {

}